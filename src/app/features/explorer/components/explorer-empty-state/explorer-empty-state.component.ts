import { ChangeDetectionStrategy, Component, input, output } from '@angular/core';
import { LucideAngularModule, SearchX } from 'lucide-angular';

@Component({ selector: 'app-explorer-empty-state', imports: [LucideAngularModule], changeDetection: ChangeDetectionStrategy.OnPush, template: `<div class="grid min-h-64 place-items-center rounded-card border border-border bg-white p-6 text-center shadow-card"><div><span class="mx-auto grid size-14 place-items-center rounded-full bg-primary-soft text-primary"><lucide-angular [img]="searchIcon" [size]="27"/></span><p class="mt-4 font-bold">{{ title() }}</p><p class="mt-1 text-sm text-ink-secondary">{{ detail() }}</p><div class="mt-4 flex justify-center"><button type="button" (click)="action.emit()" class="inline-flex min-h-11 items-center gap-2 rounded-lg bg-primary px-4 font-bold text-white hover:bg-primary-dark"><lucide-angular [img]="searchIcon" [size]="16"/>{{ actionLabel() }}</button></div></div></div>` })
export class ExplorerEmptyStateComponent { readonly title = input.required<string>(); readonly detail = input.required<string>(); readonly actionLabel = input('Nueva búsqueda'); readonly action = output<void>(); protected readonly searchIcon = SearchX; }
