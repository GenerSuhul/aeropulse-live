import { ChangeDetectionStrategy, Component, output } from '@angular/core';
import { LucideAngularModule, Radar } from 'lucide-angular';

@Component({ selector: 'app-empty-state', imports: [LucideAngularModule], changeDetection: ChangeDetectionStrategy.OnPush, template: `<div class="absolute inset-0 z-[5] grid place-items-center bg-white/85 p-6 text-center"><div><span class="mx-auto grid size-14 place-items-center rounded-full bg-primary-soft text-primary"><lucide-angular [img]="radarIcon" [size]="27"/></span><p class="mt-4 font-bold">No se detectaron aeronaves dentro del radio seleccionado.</p><p class="mt-1 text-sm text-ink-secondary">Aumenta el radio, cambia la ubicación o intenta actualizar.</p><div class="mt-4 flex flex-wrap justify-center gap-2"><button (click)="increaseRadius.emit()" class="min-h-11 rounded-lg border border-border bg-white px-4 font-bold text-ink-secondary">Usar 250 NM</button><a href="#radar-query-panel" class="inline-flex min-h-11 items-center rounded-lg border border-border bg-white px-4 font-bold text-ink-secondary">Cambiar ubicación</a><button (click)="refresh.emit()" class="min-h-11 rounded-lg bg-primary px-4 font-bold text-white">Actualizar</button></div></div></div>` })
export class EmptyStateComponent { readonly refresh = output<void>(); readonly increaseRadius = output<void>(); protected readonly radarIcon = Radar; }
