import { ChangeDetectionStrategy, Component, input, output } from '@angular/core';
import { CircleAlert, LucideAngularModule, RefreshCw } from 'lucide-angular';
import { RadarError } from '../../models/radar-error.model';

@Component({ selector: 'app-error-state', imports: [LucideAngularModule], changeDetection: ChangeDetectionStrategy.OnPush, template: `<div class="absolute inset-x-4 top-4 z-10 rounded-xl border border-red-200 bg-danger-soft p-4 shadow-panel" role="alert"><div class="flex items-start gap-3"><lucide-angular [img]="alertIcon" [size]="20" class="mt-0.5 text-danger"/><div class="flex-1"><p class="font-bold text-danger">No se pudo actualizar el radar</p><p class="mt-1 text-sm text-red-800">{{ error().message }}</p><div class="mt-3 flex flex-wrap gap-2"><button (click)="retry.emit()" class="inline-flex min-h-11 items-center gap-2 rounded-lg bg-danger px-3 text-sm font-bold text-white"><lucide-angular [img]="refreshIcon" [size]="16"/>Reintentar</button><button (click)="useMock.emit()" class="min-h-11 rounded-lg border border-red-200 bg-white px-3 text-sm font-bold text-danger">Usar datos simulados</button></div></div></div></div>` })
export class ErrorStateComponent { readonly error = input.required<RadarError>(); readonly retry = output<void>(); readonly useMock = output<void>(); protected readonly alertIcon = CircleAlert; protected readonly refreshIcon = RefreshCw; }
